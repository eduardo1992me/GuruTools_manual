/// <reference types="react" />
import { VariantProps } from "../theme";
import { TRangeCalendarProps } from "./calendar.types";
import { CalendarContainer } from "./calendar.styles";
declare function RangeCalendar({ rounded, as, colorScheme, monthDateFormat, withSelectors, tooltip, dateUnavailableStyle, size, prevIcon, nextIcon, ...props }: TRangeCalendarProps & VariantProps<typeof CalendarContainer>): JSX.Element;
export default RangeCalendar;
