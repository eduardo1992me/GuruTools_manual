/// <reference types="react" />
import { TCalendarHeader } from "./calendar.types";
export declare function CalendarHeader({ state, index, calendarProps, prevButtonProps, nextButtonProps, prevIcon, nextIcon, }: TCalendarHeader): JSX.Element;
