import { ReactNode } from "react";
declare const ScrollAreaWrapper: ({ width, height, as, children, }: {
    width?: string | number | undefined;
    height?: string | number | undefined;
    as: "single" | "double" | "infinite";
    children: ReactNode;
}) => JSX.Element;
export default ScrollAreaWrapper;
