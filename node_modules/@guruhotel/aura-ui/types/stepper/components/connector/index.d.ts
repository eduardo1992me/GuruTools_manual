import * as React from "react";
import { Box } from "../../../box";
declare type BoxProps = React.ComponentProps<typeof Box>;
interface ConnectorProps extends BoxProps {
    isCompletedStep: boolean;
    isLastStep?: boolean;
    hasLabel?: boolean;
    index: number;
}
export declare const Connector: React.MemoExoticComponent<({ isCompletedStep, children, isLastStep }: ConnectorProps) => JSX.Element>;
export {};
